const tuvi = require('tuvi-neo');
console.log(Object.keys(tuvi));
// I need to find the correct parameter format. Let's dump the function toString if possible.
console.log(tuvi.generateLaSo.toString());
